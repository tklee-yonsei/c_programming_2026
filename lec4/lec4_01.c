#include <stdio.h>

    /// @brief Calculates the average of two double values
    /// @param a First number
    /// @param b Second number
    /// @return The average of a and b
double get_average(double a, double b) {
    return 0;
}

int main() {
    double avg = get_average(5.0, 10.0);
    return 0;
}